---
quick_id: 260809-ft6
slug: remove-pet
date: 2026-08-09
---

# Quick Task 260809-ft6: 去除宠物部分

## 任务描述

移除应用中「宠物」功能：侧边栏导航入口、路由、宠物页面。宠物页当前为占位页（`<div>1</div>`），无后端引用。

## 变更范围

- `src/components/AppSidebar.vue`：删除 `navItems` 中 `/pet` 项、`routeTitleMap` 中 `/pet` 项、`Grid` icon 导入
- `src/router/index.ts`：删除 `PetPage` 导入与 `/pet` 路由
- `src/views/pet/`：删除整个目录

## 验收

- `npx vue-tsc --noEmit` 通过
- 代码中无 `宠物` / `/pet` / `PetPage` / `Grid`（sidebar）残留
- 侧边栏不再显示「宠物」，直接访问 `/pet` 无页面

## 提交

仅提交源码变更（不提交 `backend/target/classes/*.class`）
