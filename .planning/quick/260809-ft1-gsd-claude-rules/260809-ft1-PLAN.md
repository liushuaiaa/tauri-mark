---
quick_id: 260809-ft1
slug: gsd-claude-rules
description: 当前项目运行 GSD 指令时，遵循项目 .claude/rules 下的所有规则（项目本地实现）
date: 2026-08-09
mode: quick
---

# Quick Task 260809-ft1: 当前项目 GSD 遵循 .claude/rules

## 背景

用户希望在**当前项目**运行 GSD 相关指令时，GSD 子代理（planner/executor/reviewer/verifier）能够读取并遵循项目 `.claude/rules/` 下的规则（`api.md`、`frontend.md`、`components.md`），避免产出与规则不一致。

**范围约束（用户明确要求）**：不改动 `~/.claude/get-shit-done/` 下的任何全局系统文件，只做项目本地改动。

**机制说明**：GSD 所有工作流（quick.md / plan-phase.md / execute-phase.md）在子代理的 `<files_to_read>` 中都会带上 `./CLAUDE.md (Project instructions...)`。因此在项目 `CLAUDE.md` 中加入明确的强制指令，即可让所有 GSD 子代理读取 `.claude/rules/`，无需修改全局文件。

## 变更

### 1. `d:\xn-project\tauri-mark\CLAUDE.md`
在「代码规范」小节追加强制要求：
- 明确 GSD 子代理执行任务时必须**首先**读取并遵循 `.claude/rules/` 下**所有** `.md` 规则文件
- 声明这些规则优先级高于 GSD 通用默认值，规划/实现/校验阶段不得跳过

### 2. `~/.claude/get-shit-done/` 全局文件
**不改动**。之前的全局改动已不存在（已还原为原始内容），不涉及回滚。

## 验收

- [ ] 项目 `CLAUDE.md` 包含 GSD 子代理必须读取 `.claude/rules/` 的强制指令
- [ ] 未改动任何 `~/.claude/get-shit-done/` 全局文件
- [ ] `.claude/rules/` 下所有规则文件被项目内 GSD 子代理读取并遵循

## 变更文件

- `d:\xn-project\tauri-mark\CLAUDE.md`
